---
- name: Create Radiusadius Failed Vlan (enable mode commands)
  hosts: "{{ cisco_router }}"
  connection: network_cli
  gather_facts: no

  tasks:
    - name: Create Radiusadius Failed Vlan
      ios_command:
        commands:
          - "show version"

      register: print_output

    -  debug: var=print_output
